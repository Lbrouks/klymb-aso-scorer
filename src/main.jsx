import React from 'react'
import ReactDOM from 'react-dom/client'
import ASOScorer from './ASOScorer.jsx'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <ASOScorer />
  </React.StrictMode>
)
